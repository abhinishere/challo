class UserInfoModel {
  final String? uid, username, name, email, pic, selectedRadioStand;
  final bool? didStart, profileverified;

  UserInfoModel({
    this.uid,
    this.username,
    this.name,
    this.email,
    this.pic,
    this.selectedRadioStand,
    this.didStart,
    this.profileverified,
  });
}
