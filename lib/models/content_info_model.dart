class ContentInfoModel {
  final String? subject, description, category, formattype;
  final int? guestsno;

  ContentInfoModel(
      {this.subject,
      this.description,
      this.category,
      this.formattype,
      this.guestsno});
}
